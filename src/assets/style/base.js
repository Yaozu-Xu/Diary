import { StyleSheet } from 'react-native';

const baseStyle = StyleSheet.create({
  bottomTabs: {
    fontSize: 18,
  },
  shadow: {
    shadowColor: '#000',
    shadowOffset: {
      width: 0,
      height: 3,
    },
    shadowOpacity: 0.29,
    shadowRadius: 4.65,
    elevation: 7,
  },
});

export default baseStyle;
